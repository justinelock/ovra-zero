---
description: 新增/修改代码须写详细注释、更新变更日志；Git 提交信息优先中文
alwaysApply: true
---

# 代码注释、变更日志与提交规范

凡**新增或修改**业务代码、配置、脚本（非纯格式化/重命名），必须同时完成以下三项。

## 1. 详细注释（步骤级优先）

- **必须**：在新增/改动的函数、复杂逻辑块、非显而易见的分支上写注释。
- **方法外**：可写一行或多行**总结**（业务意图、与 Java/前端的对应关系），不要在外部罗列 `1. 2. 3.` 全流程。
- **方法内**：步骤说明写在**对应代码行旁**，读者读到该行即懂；可用简短句，不必编号堆在函数开头。
- **说明什么**：业务意图、前置条件、边界情况；不要复述代码字面意思。
- **何时可省略**：getter/setter、纯透传、与周围代码完全同模式的重复片段。

```go
// 按主键 id 查询租户详情（非 tenant_id 业务编号）
func (l *InfoLogic) Info(req *types.IdReq) (*types.TenantBase, error) {
	// 按 sys_tenant.id 查询
	row, err := l.svcCtx.Dal.SysTenantDal.SelectById(l.ctx, req.Id)
	if err != nil {
		return nil, err
	}
	if row == nil {
		return nil, errx.New("数据不存在")
	}
	return mapTenant(row), nil
}
```

```ts
// 页面切换动画：项目默认覆盖 Vben 的 fade-slide，见 transition.css
transition: {
  name: 'fade', // 纯淡入淡出；可选 fade-slide | fade-up | fade-down
},
```

## 2. 变更日志（必须）

- **文件**：仓库根目录 `CHANGELOG.md`（不存在则创建）。
- **时机**：每次有功能/修复/配置变更时，在 **`[未发布]`** 或当日日期分组下追加条目。
- **格式**（中文）：

```markdown
## [未发布]

### 修复
- **租户管理**：详情接口按主键 id 查询，修复编辑时「数据不存在」（`app/system/internal/dal/sys_tenant.go`）

### 新增
- ...

### 变更
- ...
```

- 每条写清：**模块/功能** + **做了什么** + **关键文件路径**（1～3 个）。
- 仅文档/注释/规则且无行为变化：可写在 `### 文档` 或 `### 维护`。

## 3. Git 提交（用户要求提交时）

- **提交说明优先使用中文**；说明「为什么改」而非只列文件名。
- 推荐格式：

```text
修复租户编辑时按 tenant_id 误查导致数据不存在

- SelectById/Delete/UpdateStatus 改按主键 id 查询
- 更新 CHANGELOG.md
```

- 不要提交密钥、`.env` 本地私密内容；不要 `--no-verify` 除非用户明确要求。

## 执行顺序（Agent）

1. 实现改动并补充步骤级注释  
2. 更新 `CHANGELOG.md`  
3. 若用户要求 commit：用中文写 message，并确认已含 CHANGELOG 更新
